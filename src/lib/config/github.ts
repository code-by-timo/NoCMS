import type { GitHubConfig } from '$lib/utils/github-api';

/**
 * GitHub repository configuration
 * Update these values to match your repository
 */
export const githubConfig: GitHubConfig = {
	owner: 'knuspermixx',
	repo: 'NoCMS',
	branch: 'main'
};
